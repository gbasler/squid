package squid.classembed

/**
  * TODO way to inline class fields at a phase
  * TODO when detecting an assert statically wrong, should abort compile
  * TODO make phases classes so we can reliably reflect on and abstract them;
  *   add possible customization methods? (or rather mixins)
  *   use extends to make hierarchies
  * 
  */
package object geom {
  
  type Num = Double
  
  
  
}
